# Scoped panel fixes on v1.0

Requested after the complete v1.0 restoration: phone camera/daylight positions and tablet layers meeting the header. Desktop layout is unchanged.

Only new CSS064 and JS408 plus their template includes are added to the v1.0 source. No changes to render code, navigation, vehicles, button labels, jump, toolbar membership, panel sizing or feature exclusivity.

- Tablet layer top follows the actual header bottom, including orientation changes.
- Tablet top tools use the space between the layer and camera panels to avoid covering the layer header. Layer expand/collapse does not change this reserved width. Desktop and bottom tools remain unchanged.
- Phone camera retains its right edge, clearing actual intersecting top controls. Phone daylight remains right-aligned. Available height accounts only for controls intersecting the panel horizontally; content scrolls without repositioning controls.
- A bounded resize settle pass handles the original delayed viewport layout. There is no continuous frame-loop work.

Verification: `node scripts/test-v1-panel-anchors.mjs` passes52 checks over12 touch viewports plus desktop, collapsed/expanded layers and rotation with tools open; no page errors. Screenshots reviewed in docs/v1-panel-review. v1 release/embedded asset tests6 pass, design audit29/0, build and diff check pass.

Initial scoped patch was published: offline75c6ebd, online38a59a3, standalone Worker dd894d71-e87a-436c-81ee-9f01b0b75cac. Both public endpoints passed52 layout checks.

## Landscape and touch follow-up

- Reproduced iPad layer disappearance at1024x650 and1180x650. Existing iPad detection now preserves tablet layers and quality control even when browser chrome shortens the viewport. Header anchoring is retained; daylight clears the top controls.
- Fixed the touch document root and made canvas touchmove cancellation non-passive. Panel content remains independently scrollable.
- Reproduced8px finger jitter cancelling navigation and rotating the camera. Touch tolerance is12px, mouse remains5px; rotation waits until the touch leaves the tap tolerance. Long-press preview uses the same threshold. Cancellation clears the rotation owner.
- Explicit tap can retry saved collision preparation, without source baking. It resumes only if the scene, camera position, view direction and input ownership still match. Missing proxy data still requires preparation; this patch does not silently regenerate large scenes.
- Public demo loaded its482369-byte collision proxy and reached ready during read-only verification. The user's affected scene has not yet been identified.

Verification: Chrome64/64 and desktop WebKit64/64 panel checks, including document scroll offsets, both orientations, short-height iPad and tools open. Screenshots inspected. Input/physics/whole-cache tests50 pass; online synchronization gate308 pass,1 skipped,1 TODO; design audit29/0.

These are emulated browser checks, not physical iPhone/iPad validation. WebKit was used for layout only. No Dropbox copies changed in this follow-up. Publication verification is recorded after deployment below.

### Publication verified

- Source push12c281a; online push15f100a. Online Actions34800908699 succeeded.
- Standalone Worker eb0bdbc8-8b8b-4b6c-ac2c-fc4c3cd59e75, release5f70f21b25c8b5b5fc005cb2ddf4ec351109a91d221abe0f0508e92951bd8c6f.
- Online release64be9018b9e43d2553a43611e61d176955eaa5596940b2dc671b032eacb40aea.
- Each public endpoint passed64 layout checks with zero page errors. Public demo render was nonblank and visually reviewed; file-origin automatic update and asset hashes passed.
- First standalone promotion failed its live startup check and automatically rolled back. An unchanged-source retry passed the full guarded deployment and live startup verification. The initial failure's exact cause was not established.
- Actual browser/Rapier navigation test passed: eased mouse/touch travel, held target preview, wall blocking, wall-to-floor targeting, measurement taps and responsive tool taps. Evidence: F:/Codex/3dgs-renderer-research-20260911/click-browser-1789354798422.
- Remaining limits: physical iOS is unverified; user has not identified the scene that remains unready. Missing collision proxies are deliberately not baked on ordinary taps, preserving the requested render-performance rollback.
